{
  "name": "react-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "env-cmd -r .env-rc.js -e development react-scripts start",
    "build:test": "env-cmd -r .env-rc.js -e test react-scripts build",
    "build:production": "env-cmd -r .env-rc.js -e production react-scripts build"
  },
  "dependencies": {
    "history": "^4.10.1",
    "react": "16.13.1",
    "react-dom": "16.13.1",
    "react-router-dom": "^5.2.0",
    "react-scripts": "4.0.2",
    "styled-components": "^5.2.1",
    "styled-normalize": "^8.0.7",
    "packages-hooks": "^0.1.0",
    "packages-store": "^0.1.0"
  },
  "devDependencies": {
    "env-cmd": "^10.1.0"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
